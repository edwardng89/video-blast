<div class="ibox">
  <!-- ibox body -->
  <div class="ibox-body">
    <div class="ibox-filters">
      <p class="required-field">All fields marked with&nbsp;<span class="red-asterisk">*</span>&nbsp;are required</p>
    </div>
    <div class="ibox-content">
      <!-- fieldset code here -->  
      <div class="row mb20">
        <!-- fields go here -->
        <% if can_access_attribute?(:order) %>
          <div class="col-sm-4">
            <div class="form-group selectdiv">
              <div class="control-wrapper">
                <%= f.association :order, collection: select_list(Order.all,
                                           f.object.order, 'references', 'to_s'), label: '<span></span> Order'.html_safe,
          input_html: {
            class: ' select2 form-control',
            data: { autocomplete_url: admin_orders_path,
                    selection_length: Order.all.count,
                    text_identifier: 'to_s',
                    max_length: ENV['LIST_MAX_RECORDS'].to_i > 0 ? ENV['LIST_MAX_RECORDS'].to_i : 50 }
          }, readonly: cannot_modify_attribute?(:order) %>
              </div>
            </div>
          </div>
        <% end %>
        <% if can_access_attribute?(:movie_copy) %>
          <div class="col-sm-4">
            <div class="form-group selectdiv">
              <div class="control-wrapper">
                <%= f.association :movie_copy, collection: select_list(MovieCopy.all,
                                           f.object.movie_copy, 'references', 'to_s'), label: '<span></span> Movie Copy'.html_safe,
          input_html: {
            class: ' select2 form-control',
            data: { autocomplete_url: admin_movie_copies_path,
                    selection_length: MovieCopy.all.count,
                    text_identifier: 'to_s',
                    max_length: ENV['LIST_MAX_RECORDS'].to_i > 0 ? ENV['LIST_MAX_RECORDS'].to_i : 50 }
          }, readonly: cannot_modify_attribute?(:movie_copy) %>
              </div>
            </div>
          </div>
        <% end %>
        <% if can_access_attribute?(:returned_on) %>
          <div class="col-sm-4">
            <div class="form-group">
              <div class="control-wrapper">
                <%= f.input :returned_on, as: :string, label: '<span></span> Returned On'.html_safe,
                  input_html: { class: ' form-control ', value: f.object&.returned_on&.strftime('%Y-%m-%d'),
                                type: 'date'  },
                  readonly: cannot_modify_attribute?(:returned_on) %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
      <!-- nested form code here -->
      <%= f.hidden_field :alt_list, value: @alt_list %>
    </div>
  </div>
  <!-- /- ibox body -->
</div>
