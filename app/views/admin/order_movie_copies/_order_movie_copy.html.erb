<tr  >
  <!-- table columns here  -->
  <% if can_access_attribute?(:order) %>
    <td class="text-left  ">
      <%= order_movie_copy.order&.decorate&.to_s %>
    </td>
  <% end %>
  <% if can_access_attribute?(:movie_copy) %>
    <td class="text-left  ">
      <%= order_movie_copy.movie_copy&.decorate&.to_s %>
    </td>
  <% end %>
  <% if can_access_attribute?(:returned_on) %>
    <td class="text-right   minimal">
      <%= order_movie_copy.returned_on %>
    </td>
  <% end %>
  <% if can?(:update,  (order_movie_copy.respond_to?(:object) ? order_movie_copy.object : order_movie_copy) ) %>
    <% icon = '<i class="fas fa-pencil-alt" aria-hidden="true"></i>' %>
    <% full_icon = '<i class="fas fa-edit" aria-hidden="true"></i>' %>
    <% form_path = edit_polymorphic_path([:admin, order_movie_copy], alt_list: 'order_movie_copy' ) %>
  <% elsif can?(:read,  (order_movie_copy.respond_to?(:object) ? order_movie_copy.object : order_movie_copy) )  %>
    <% icon = '<i class="fas fa-search" aria-hidden="true"></i>' %>
    <% full_icon = '
      <span class="fa-stack">
        <i class="fa fa-search fa-stack-1x"></i>
      </span>' %>
    <% form_path = polymorphic_path([:admin, order_movie_copy], alt_list: 'order_movie_copy') %>
  <% end %>
  <td class="minimal">
    <%= link_to('<span class="fa-stack"><i class="fa fa-search fa-stack-1x"></i></span>'.html_safe,
                polymorphic_path([:admin, order_movie_copy]),
        title: 'Show order_movie_copy', class: 'index-link') %>
    <% if can?(:update, (order_movie_copy.respond_to?(:object) ? order_movie_copy.object : order_movie_copy) ) %>
      <%= link_to(full_icon.html_safe,
                  edit_polymorphic_path([:admin, order_movie_copy]),
          title: 'Edit order_movie_copy',
          class: 'index-link') %>
    <% end %>
    <% if can?(:update, (order_movie_copy.respond_to?(:object) ? order_movie_copy.object : order_movie_copy) ) %>
      <%= link_to icon.html_safe, '#', title: 'Edit order_movie_copy',
                  class: 'index-link modal-btn modal-edit', data: { 'form-url': form_path, alt_list: action_name } %>
    <% end %>
  </td>
  <!-- action buttons start -->
  <!-- action buttons end -->
</tr>
