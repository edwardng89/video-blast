<!-- fieldset code here -->  
<div class="row mb20">
  <!-- fields go here -->
  <% if can_access_attribute?(:movie) %>
    <div class="col-lg-6">
      <div class="form-group">
        <div class="control-wrapper">
          <div class='input'>
            <%= label_tag 'Movie', 'Movie:' %>
            <div class="form-control read-only text-left">
              <%= movie_genre.movie %></div>
          </div>
        </div>
      </div>
    </div>
  <% end %>
  <% if can_access_attribute?(:genre) %>
    <div class="col-lg-6">
      <div class="form-group">
        <div class="control-wrapper">
          <div class='input'>
            <%= label_tag 'Genre', 'Genre:' %>
            <div class="form-control read-only text-left">
              <%= movie_genre.genre %></div>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>
<!-- action list start -->
<!-- action list end -->
